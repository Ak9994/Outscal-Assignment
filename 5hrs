Basic variables:
Age = 20
Max Age = 100
timelimit = 5 
Spawn_limit_per_hour = 20
time = 5
boolean check : Is_Mirrored / Not_Mirrored
--------------------------------------------------------------------------------------
5 hrs to escape from mirror dimension with one base starting point and 5 levels to complete.
Randomly generate stage based on dice roll from 1-6 

ObjectParameters: Id , Passengers, vacancy

End_Result: Fails if Age == Max Age && Hour == timeLimit
            Success if Loops ends under Age <= Max Age && hour < timelimit

Logic: 

loop: while hour != timeLimit then
            switch hour then
                case 1: for time = hr, time <= 1 , time ++ then 
                            if correct_choice , exit for loop and display success
                            else if wrong_choice , increment hour by 1
                                                   increment age by 20
                                            and exit for loop
                case 2: for time = hr, time <= 2 , time ++ then 
                            if correct_choice , exit for loop and display success
                            else if wrong_choice , increment hour by 1
                                                   increment age by 20
                                            and exit for loop
                case 3: for time = hr, time <= 3 , time ++ then 
                            if correct_choice , exit for loop and display success
                            else if wrong_choice , increment hour by 1
                                                   increment age by 20
                                            and exit for loop
                case 4: for time = hr, time <= 4 , time ++ then 
                            if correct_choice , exit for loop and display success
                            else if wrong_choice , increment hour by 1
                                                   increment age by 20
                                            and exit for loop
                case 5: for time = hr, time <= 5 , time ++ then 
                            if correct_choice , exit for loop and display success
                            else if wrong_choice , increment hour by 1
                                                   increment age by 20
                                            and exit for loop
                default: if no choice is made
                            Raise age by 20
--------------------------------------------------------------------------------------
Possible mechanics:
Combat Mechanic(either turn based/free) if needed:
            use raw materials found on level & combine them to make an appropriate atk
            atk type is based on the theory behind it.

On wrong Choice: Debuff on encounter. 
Result:     Failed encounter > set fail condition and age character up
            Successful encounter > 

On Correct Choice: Buff on encounter.
Result:     Failed encounter > set fail condition and age character up
            Successful encounter >  
            
